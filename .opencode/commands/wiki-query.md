---
description: Query the wiki for summaries, comparisons, retrieval practice, or gap-checks.
---

Use the `wiki-query` skill to answer the following, citing wiki pages (and `raw/` sources when useful). Read-only unless the user agrees to file something new.

Question: $ARGUMENTS
