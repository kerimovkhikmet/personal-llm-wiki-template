---
description: Read-only semantic wiki health check for contradictions, stale claims, orphans, and broken links.
---

Use the `wiki-lint` skill. This is read-only semantic linting - output a punch list only, don't edit anything. Wait for the user to say "fix these" before changing any file.

Scope: $ARGUMENTS (leave empty to lint the whole wiki).
