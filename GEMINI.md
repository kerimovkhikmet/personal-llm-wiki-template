# Gemini CLI - this repo

Read and follow **`AGENTS.md`** in the project root first. Then:

1. Read `wiki/index.md` - the entry point, every page listed with a one-line summary.
2. Skim `wiki/log.md` for recent structural changes.
3. Follow the same `raw/` → `wiki/` workflow (capture, ingest, link, review, query, lint, discover) described in `AGENTS.md`, whether or not this tool supports opencode's slash commands or skills directly.

The wiki is durable memory across tools - don't assume the same chat/tool will resume. Don't invent a parallel structure, and don't overwrite `AGENTS.md`.
